import { useState } from "react";

export default function SearchBar({ items, onItemsSelected }) {
  const [query, setQuery] = useState("");
  const [retults, setResults] = useState([])
  return <div>Saludos</div>;
}
